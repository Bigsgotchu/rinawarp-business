version: '3.8'
services:
  rina-agent:
    build: .
    image: rinawarp/agent:latest
    ports: ['3333:3333']
    environment:
      - PORT=3333
    healthcheck:
      test: ['CMD', 'curl', '-sf', 'http://127.0.0.1:3333/health']
      interval: 10s
      timeout: 2s
      retries: 3
