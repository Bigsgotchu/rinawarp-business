{
  "name": "rinawarp-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev ./public --compatibility-date=2024-11-21",
    "deploy": "wrangler pages deploy ./public --project-name=rinawarptech"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
